bugs:

clearing on text change (prolly need to use RPM values)
reload (semifixed, need persistence for proper fix)
linux issue?
Relative to ground plane (haha, no)

features:

persistence
moduleManager
part resource consumption/production?
display target vessel
Custom rotation plane
Stress color mode (apparently forces are not exposed in unity, alas)
show up in editor?
custom color modes
action group coloring

check:

performance on giants (...someone would have complained by now right)
